import { Box, Typography } from "@mui/material";

function Layout() {
    return (
        <div id="grid">
            <div id="left"></div>
            <div id="right">The BEATLES</div>
        </div>
    )
};

export default Layout;