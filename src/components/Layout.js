import { Box, Button, Typography } from "@mui/material";

function Layout() {
    return (
        <div id="grid">
            <div id="left"></div>
            <div id="right">

            <Button>The BEATLES</Button>

               
                </div>
        </div>
    )
};

export default Layout;